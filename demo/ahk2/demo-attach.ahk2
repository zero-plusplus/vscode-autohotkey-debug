#Requires AutoHotkey v2.0+
#Warn All, StdOut
#SingleInstance

SetTimer LoopFn, 1000
LoopFn() {
  a := ""
  b := ""
  c := ""
  FileOpen("*", "w").writeLine("stdout")
  FileOpen("**", "w").writeLine("stderr")
  OutputDebug("outputdebug")
}
