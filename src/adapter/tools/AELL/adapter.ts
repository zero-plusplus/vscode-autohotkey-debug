import { Property, Session } from '../../../types/dbgp/ExtendAutoHotkeyDebugger';
import { VariableAdapter } from '../../../types/tools/AELL';

export const createVariableAdapter = (session: Session): VariableAdapter<Property> => {
  return {

  };
};
